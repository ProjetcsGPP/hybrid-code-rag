[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "code-rag"
version = "0.1.0"
description = "CODE-RAG V2 system"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["pipeline_v2"]
